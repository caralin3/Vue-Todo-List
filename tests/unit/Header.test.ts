import { mount } from '@vue/test-utils';
import Header from '@/components/Header.vue';

describe('Header', () => {
  it('was skipped', () => undefined);
  // const wrapper: any = mount(Header, {
  //   stubs: ['router-link'],
  // });

  // it.skip('renders the correct markup', () => {
  //   expect(wrapper.element).toMatchSnapshot();
  // });
});
